const server = require('./api/server.js');

const port = process.env.PORT || 5000;
server.listen(port, () => console.log(`\n** Running on port ${port} **\n`));

app.listen(, () => {
    console.log('App listening on port 3000!');
});

app.listen(6660, () => {
    console.log(`Server started on 6660`);
});

app.listen(3000, () => {
    console.log('App listening on port 3000!');
});


APP.